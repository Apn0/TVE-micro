# Mock PLC Entry Point
# This will simulate the Raspberry Pi's data stream so we can build the UI on Windows.

def main():
    """
    Main entry point for the Mock PLC simulation.

    This function prints a startup message. It is intended to serve as a placeholder
    or starting point for simulating PLC behavior (Temperature, RPM, Pressure readings)
    when running on non-hardware environments (e.g., Windows for UI development).
    """
    print("INTAREMA PLC Simulation Started...")
    # TODO: Simulate Temperature, RPM, and Pressure readings
    
if __name__ == "__main__":
    main()
